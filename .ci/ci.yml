version: 1
preset: node

delivery:
  mode: staged
  pr:
    deployToStg: false

build:
  pnpm:
    nodeVersion: 20.19.2
    packageName: "@services/svc-feed"
    steps:
      - type: lint
        name: Lint service sources
      - type: typecheck
        name: Typecheck service package
      - type: test
        name: Run unit tests
        command: test:unit
      - type: test
        name: Run integration tests
        command: test:integration
      - type: build
        name: Build service package

image:
  repository: registry.leultewolde.com/mereb/svc-feed
  context: .
  dockerfile: Dockerfile
  tagStrategy: branch-sha
  verifyPull: true
  push:
    enabled: true
    when: '!pr'
    credentialsId: docker-registry-local
    registry: http://10.0.0.222:30400

deploy:
  order:
    - dev
    - dev_outbox
    - stg
    - stg_outbox
    - prd
    - prd_outbox

  dev:
    release: svc-feed-dev
    namespace: apps-dev
    chart: app-chart
    repo: https://charts.leultewolde.com
    repoCredentialId: helm-chart-creds
    valuesFiles:
      - .ci/values-dev.yaml
    valuesTemplates:
      - template: .ci/templates/values-secret.yaml.tpl
        output: .ci/.rendered/values-dev.secret.yaml
        vars:
          SECRET_NAME: svc-feed-dev-secrets
          DATABASE_URL:
            vaultPath: kv/data/apps/dev
            vaultField: FEED_DATABASE_URL
          SPLUNK_HEC_TOKEN:
            vaultPath: kv/data/apps/dev
            vaultField: SPLUNK_HEC_TOKEN
    vault:
      url: https://vault.leultewolde.com
      tokenEnv: VAULT_TOKEN
      engine: kv2
    credentials:
      - type: vaultToken
        id: vault-credentials
        env: VAULT_TOKEN
    smoke:
      url: https://api-dev.mereb.app/feed/healthz
      retries: 3
      delay: 10
      timeout: 60s

  dev_outbox:
    release: svc-feed-dev-outbox
    namespace: apps-dev
    chart: app-chart
    repo: https://charts.leultewolde.com
    repoCredentialId: helm-chart-creds
    valuesFiles:
      - .ci/values-dev.yaml
      - .ci/values-outbox-worker.yaml
    notes: "Dedicated outbox relay worker deployment"

  stg:
    release: svc-feed-stg
    namespace: apps-stg
    chart: app-chart
    repo: https://charts.leultewolde.com
    repoCredentialId: helm-chart-creds
    valuesFiles:
      - .ci/values-stg.yaml
    valuesTemplates:
      - template: .ci/templates/values-secret.yaml.tpl
        output: .ci/.rendered/values-stg.secret.yaml
        vars:
          SECRET_NAME: svc-feed-stg-secrets
          DATABASE_URL:
            vaultPath: kv/data/apps/stg
            vaultField: FEED_DATABASE_URL
          SPLUNK_HEC_TOKEN:
            vaultPath: kv/data/apps/stg
            vaultField: SPLUNK_HEC_TOKEN
    vault:
      url: https://vault.leultewolde.com
      tokenEnv: VAULT_TOKEN
      engine: kv2
    credentials:
      - type: vaultToken
        id: vault-credentials
        env: VAULT_TOKEN
    smoke:
      url: https://api-stg.mereb.app/feed/healthz
      retries: 3
      delay: 10
      timeout: 60s

  stg_outbox:
    release: svc-feed-stg-outbox
    namespace: apps-stg
    chart: app-chart
    repo: https://charts.leultewolde.com
    repoCredentialId: helm-chart-creds
    valuesFiles:
      - .ci/values-stg.yaml
      - .ci/values-outbox-worker.yaml
    notes: "Dedicated outbox relay worker deployment"

  prd:
    release: svc-feed
    namespace: apps-prd
    chart: app-chart
    repo: https://charts.leultewolde.com
    repoCredentialId: helm-chart-creds
    valuesFiles:
      - .ci/values-prd.yaml
    valuesTemplates:
      - template: .ci/templates/values-secret.yaml.tpl
        output: .ci/.rendered/values-prd.secret.yaml
        vars:
          SECRET_NAME: svc-feed-prd-secrets
          DATABASE_URL:
            vaultPath: kv/data/apps/prd
            vaultField: FEED_DATABASE_URL
          SPLUNK_HEC_TOKEN:
            vaultPath: kv/data/apps/prd
            vaultField: SPLUNK_HEC_TOKEN
    vault:
      url: https://vault.leultewolde.com
      tokenEnv: VAULT_TOKEN
      engine: kv2
    credentials:
      - type: vaultToken
        id: vault-credentials
        env: VAULT_TOKEN
    smoke:
      url: https://api.mereb.app/feed/healthz
      retries: 3
      delay: 10
      timeout: 60s

  prd_outbox:
    release: svc-feed-outbox
    namespace: apps-prd
    chart: app-chart
    repo: https://charts.leultewolde.com
    repoCredentialId: helm-chart-creds
    valuesFiles:
      - .ci/values-prd.yaml
      - .ci/values-outbox-worker.yaml
      - .ci/values-prd-outbox-worker.yaml
    notes: "Dedicated outbox relay worker deployment"

signing:
  enabled: false

release:
  autoTag:
    enabled: true
    when: branch=main & !pr
    prefix: v
    bump: patch
    remote: origin
    gitUser: Mereb CI
    gitEmail: ci@mereb.dev
    credentialId: github-credentials
    clean: false
    allowDirty: true
    skipIfTagged: true
  github:
    enabled: true
    when: tag=^v[0-9].*
    repo: leultewolde/mereb-svc-feed
    stageName: GitHub Release
    credentialId: github-credentials
    generateReleaseNotes: true
